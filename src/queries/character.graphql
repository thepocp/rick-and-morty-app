query Character($id: ID!) {
    character(id: $id) {
        id
        name
        status
        species
        type
        gender
        origin {
            id
            name
            type
        }
        location {
            id
            name
            type
        }
        image
        episode {
            id
            name
            air_date
            episode
        }
    }
}
