query Charactes($page: Int) {
    characters(page: $page) {
        results {
            id
            name
            species
            image
        }
        info {
            next
        }
    }
}
