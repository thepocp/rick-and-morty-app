query Charactes($page: Int, $filters: FilterCharacter) {
    characters(page: $page, filter: $filters) {
        results {
            id
            name
            species
            image
        }
        info {
            next
        }
    }
}
